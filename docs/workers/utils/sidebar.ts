import type { SidebarsConfig } from "@docusaurus/plugin-content-docs";

const sidebar: SidebarsConfig = {
    utilssidebar: [
        {
            type: "doc",
            id: "workers/utils/entrypoints"
        }
    ]
};

export default sidebar.utilssidebar;
